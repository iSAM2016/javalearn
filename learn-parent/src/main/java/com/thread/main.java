package com.thread;

/**
 * 进程： 是一个正在执行中的程序， 每一个进程执行都有一个执行顺序， 该顺序是一个执行路径， 或者叫做控制单元
 *
 * 线程： 就是进程中的一个独立的控制单元，线程在控制着进程的执行
 *
 * 一个线程中至少存在一个线程。
 *
 * java vm f启动的时候会有个进程java.exe
 *
 * 该进程中至少有一个负责java 程序的执行，而且这个线程运行的代码存在于main 方法中。
 *
 * 扩展： 其实更细节说明jvm 启动不知一个线程，还有负责垃圾祸首机制的线程
 */
public class main {
}
