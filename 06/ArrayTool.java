class ArrayTool {
    // public int getMax(int[] arr) {
    // int max = 0;
    // return arr[max];
    // }
    private ArrayTool() { // 将构造函数私有化，不能new l
    }

    public static int getMax(int[] arr) {
        int max = 0;
        return arr[max];
    }
}